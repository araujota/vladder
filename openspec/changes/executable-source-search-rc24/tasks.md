## 1. Closure Model

- [x] 1.1 Add stage-specific executable closure and finite parameter-domain schemas.
- [x] 1.2 Add conservative contract inference and sound family applicability registry.
- [x] 1.3 Replace callable-only source-route coverage with executable-stage coverage.

## 2. Unified Search

- [x] 2.1 Connect compiler/source semantic roots directly to grammar dispatch.
- [x] 2.2 Implement lazy partial-state expansion with EXPAND/DEFER/PRUNE policy hooks.
- [x] 2.3 Separate deterministic impossibility, canonical memoization, and heuristic pruning.
- [x] 2.4 Enumerate concrete candidates and composition descendants deterministically in shadow mode.
- [x] 2.5 Compile, prove, assembly-deduplicate, and cache every concrete terminal.
- [x] 2.6 Emit complete v3 lineage and terminal observations from the same search.
- [x] 2.7 Add deterministic multi-root parallel execution and resumability.

## 3. Grammar Expansion

- [x] 3.1 Add ordered prefix/suffix reduction extraction, lowering, and proof.
- [x] 3.2 Bind bounded compaction, codecs, AoS reductions, and stateful deltas automatically.
- [x] 3.3 Add finite retained-cache/lifetime and no-growth ownership candidate plans.
- [x] 3.4 Add definition-visible cross-TU composition lineage and closure.
- [x] 3.5 Emit explicit bounded protocol branches for concurrency, GPU, and external boundaries.

## 4. Product Integration

- [x] 4.1 Use unified search in canonical normal optimization routing.
- [x] 4.2 Add CLI commands and concise executable-closure summary artifacts.
- [x] 4.3 Make terminal normal workflows emit the same authoritative v3 trace when consent permits.
- [x] 4.4 Update README, skill, schemas, and release evidence.

## 5. Acceptance

- [x] 5.1 Add end-to-end source-to-proof fixtures for each executable closure class.
- [x] 5.2 Run full tests and OpenSpec validation.
- [x] 5.3 Run a monitored four-project campaign and audit labels, lineage, and family balance.
- [x] 5.4 Train/evaluate the pruning model only after executable positive-path and grammar-diversity evidence exists. The initial campaign rejected training; the expanded exhaustive campaign trained a shadow model, whose held-out evaluation rejected live eligibility.

## 6. Production Selected-Build Closure

- [x] 6.1 Compose independently proved C++ regions lazily into complete translation-unit candidates.
- [x] 6.2 Expand every selected production compile configuration and preserve it as a distinct root.
- [x] 6.3 Isolate parallel roots in worker processes so compiler and solver failures cannot corrupt sibling lineage.
- [x] 6.4 Canonicalize duplicate AST regions and retain internal-linkage symbols in a separate identity build.
- [x] 6.5 Add mutually exclusive unroll, vector-width, and interleave schedule realizations with typed action lineage.
- [x] 6.6 Re-run the proof-dense multi-project source inventory and audit executable-root, terminal, label, and grammar-action balance.
- [x] 6.7 Add an exact fixed-width bit-popcount reduction grammar and Rust lowering for real DataFusion bitmap code.
- [x] 6.8 Add a persistent fail-open pruning-oracle protocol that intervenes before partial-state expansion.
- [x] 6.9 Emit schema-validated v3 training bundles automatically from every manifest campaign root.
- [x] 6.10 Gate terminal candidates before source/proof/compile realization and persist per-root v3 progress.
- [x] 6.11 Carry action lineage into every live policy query and add a stage-specific shadow trainer/oracle reference.
- [x] 6.12 Move automatic grammar-family dispatch into the real lazy tree before family expansion.
- [x] 6.13 Separate deterministic, canonicalized, synthetic, and learned-policy decision surfaces in v3 supervision.
- [x] 6.14 Defer selected-build schedule source, proof, and compile materialization until its lazy action survives.
- [x] 6.15 Add complete-module selected-function LLVM pipelines with two-module Alive2 refinement and selected-symbol assembly deduplication.
- [x] 6.16 Separate local semantic capsule closure from effect-preserving compiler schedules so owning, callback, allocation, and atomic loops can be enumerated without claiming generic protocol equivalence.

## 7. Decision-State Supervision

- [x] 7.1 Add a language-neutral pre-decision state graph with selected-region focus and semantic deltas.
- [x] 7.2 Carry the exact same decision context through the live oracle protocol and authoritative v3 lineage.
- [x] 7.3 Make graph-learning examples and the reference model encode focused partial state rather than only the root graph.
- [x] 7.4 Classify negative terminal causes and audit which causes are learnable before expansion.
- [x] 7.5 Re-run root- and project-held-out evaluation before granting live pruning eligibility. Evaluation rejected live eligibility: 112/432 useful paths were missed at the tested policy point.

## 8. Canonical Native Closure

- [x] 8.1 Lower pointwise, guarded, stencil, scan, recurrence, and indirect-memory regions through one shared executable grammar.
- [x] 8.2 Emit native C, C++, Rust, Zig, and Julia candidates from the same canonical derivations.
- [x] 8.3 Corroborate source capture with each language's native compiler IR before candidate generation.
- [x] 8.4 Prove bounded schedule coverage, run native differential tests, compile, and assembly-deduplicate every terminal.
- [x] 8.5 Connect canonical capture to automatic source-family dispatch and authoritative v3 lineage.

## 9. Exhaustive Corpus Scaling

- [x] 9.1 Enumerate complete Cartesian products inside bounded executable regional grammars.
- [x] 9.2 Retain proof failures, compiler identities, duplicates, and exhausted descendants as typed terminal outcomes.
- [x] 9.3 Parallelize independent terminal realization without changing lazy decision order or label authority.
- [x] 9.4 Complete the four-project campaign and publish executable-versus-blocked coverage and label balance.

## 10. C++ Replacement And Structural-Lineage Closure

- [x] 10.1 Use Clang-selected source ranges as the authority for C++ grammar recognition and make the lexical fallback comment/string/raw-string safe.
- [x] 10.2 Separate executable proof units from complete owning-source replacements in closure reports and campaign coverage.
- [x] 10.3 Represent runtime-sized borrowed ranges without inventing a finite source maximum; prove finite-block composition structurally.
- [x] 10.4 Preserve exact input-extent versus selected-extent capacity-failure semantics.
- [x] 10.5 Add runtime overlap dispatch with a baseline-order fallback for inferred C++ compaction contracts.
- [x] 10.6 Emit width-correct 8/16/32/64-bit C++ compaction and state-delta proof units.
- [x] 10.7 Reconstruct exact-ABI free C++ compaction and codec definitions under the selected production compile command.
- [x] 10.8 Expose multi-stage dataflow structure as lazy ancestors and propagate terminal utility through v3 lineage.
- [x] 10.9 Re-run the C++-primary multi-project campaign and audit replacement-ready, proof-unit, blocked, and uncertain records.
- [x] 10.10 Re-train and held-project evaluate the pruning model only if the completed C++ corpus meets the useful-descendant coverage gate. The 3,254-positive corpus cleared the evidence gate; held-project evaluation remained shadow-only at 98.74% recall and 19.51% branch reduction because 41 useful branches were missed.
- [x] 10.11 Select overloaded C++ definitions by an authoritative source line as well as by a manually supplied mangled symbol.
- [x] 10.12 Generate deterministic family-stratified C++ campaign manifests with duplicate-root exclusion and compile-database provenance.
- [x] 10.13 Honor both `workers` and the documented `root_workers` alias so multi-root campaigns execute concurrently.
- [x] 10.14 Mark proved, compiling selected-build translation units as replacement-ready terminals while keeping mixed dispatch-root closure conservative.
- [x] 10.15 Resolve bounded Clang/LLVM ABI alias chains and classify typedef parameters from canonical types while preserving source provenance.
- [x] 10.16 Bound exhaustive selected-build composition to an explicit region count for corpus campaigns and report every omitted eligible region outside the claim envelope.
- [x] 10.17 Add explicit full-versus-decisive campaign artifact retention so exhaustive C++ supervision can scale without discarding decisive root evidence or v3 lineage.
- [x] 10.18 Fingerprint and preload per-root v3 progress so interrupted exhaustive campaigns resume without transiently losing completed supervision.
- [x] 10.19 Discover additional non-overlapping C++ roots from strong symbols in the exact built objects while preserving compile-command and project provenance.
- [x] 10.20 Treat selected-build regional prewarming as a non-authoritative cache optimization: record per-choice failures and continue terminal/root evaluation fail closed.
- [x] 10.21 Cache deterministic regional materialization failures by source, closure, compile command, region, and choice so exhaustive terminal lineage does not repeatedly compile the same invalid capsule.
- [x] 10.22 Convert Clang extraction timeouts into typed C++ adapter evidence so expensive translation units fail closed without aborting corpus campaigns.
- [x] 10.23 Generate auditable top-up manifests from useful completed roots by selecting explicit, previously omitted C++ region domains instead of weakening the model-evidence gate.
