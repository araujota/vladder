# Design: Pipeline End-To-End V4

The CLI uses content-addressed analyze, search, compile, verify, benchmark, integrate,
and report stages. Resumption requires matching artifacts. Reports render before/after
graphs and edge traffic tables without implying that modeled cache placement occurred.
