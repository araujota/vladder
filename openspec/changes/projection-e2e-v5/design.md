# Design: Projection End-To-End V5

Every stage consumes immutable manifests and emits hashes. Static synthesis can report
`best_verified_found` but must report physical measurement as `NOT_RUN`. Production
ranking additionally requires transformed-weight provenance, exact model proof,
independent-process statistics, and every portfolio component.
