# Design: LLM Projection Integration V5

The pinned Qwen3-4B Q4_K_M artifact is the initial production corpus. Gate/up and
Q/K/V complexes are extracted by tensor names and graph dependencies, not assumed
from layer count. Existing native Q4_K repacking is recorded as a baseline layout.
Additional models and formats are required before transfer claims.
