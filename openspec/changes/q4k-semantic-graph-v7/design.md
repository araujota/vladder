# Design: Production Q4KKernelGraph V7

The graph is below ProjectionComplexGraph and models physical packed execution rather
than only the mathematical dot product. E1 freezes production operation order. Layout
proofs are byte-level bijections with deterministic forward and inverse hashes. E2 is
represented but cannot be reported as exact and is unused by the first production search.
