# Automatic Region Validation Workspace

This workspace is independent of NeuralFusion and exercises every region class admitted by
`bounded-regions-v1`. Files prefixed `supported_` must enter automatic source lowering. Files
prefixed `adapter_` must fail closed with the named adapter requirement.
